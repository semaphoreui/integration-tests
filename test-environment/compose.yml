name: semaphore-test

# Backward-compatible SQLite entrypoint. Managed profiles use their manifests
# under test-environment/profiles and reuse the same common Compose definition.
include:
  - path:
      - compose.base.yml
      - profiles/core-sqlite-local/compose.yml
