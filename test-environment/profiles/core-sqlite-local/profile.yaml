id: core-sqlite-local
description: Community develop Docker image with SQLite and task execution inside the server container
compose_file: ../../compose.yml
compose_project: semaphore-test
readiness_url: http://localhost:3000/api/ping
stand: semaphore
semaphore_version: develop
semaphore_image: semaphoreui/semaphore-community:develop
# This tag moves with develop; the resolved digest and source revision come from the image.
edition: community
installation: docker-compose
architecture: native
database_type: sqlite
database_image: embedded
execution_mode: local
execution_executor: local
auth: password
git_client: cmd_git
capabilities: core-api,task-execution,task-stop,git-local,rbac,schedules,secrets
