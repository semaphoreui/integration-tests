events {}

http {
  server {
    listen 80;
    server_name fixture-git;

    location = /health {
      access_log off;
      default_type text/plain;
      return 200 "ok";
    }

    location /fixtures.git/ {
      root /srv/git;
    }
  }
}
