---
- name: External managed fixture B
  hosts: all
  gather_facts: false
  tasks:
    - name: Keep the task observable briefly
      ansible.builtin.pause:
        seconds: 3

    - name: Emit the external fixture B marker
      ansible.builtin.debug:
        msg: semaphore-bookwright-external-managed-b-ok
