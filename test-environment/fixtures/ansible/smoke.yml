---
- name: Semaphore test fixture
  hosts: all
  gather_facts: false
  tasks:
    - name: Emit deterministic smoke marker
      ansible.builtin.debug:
        msg: semaphore-bookwright-smoke-ok

    - name: Keep the process alive while Semaphore drains task output
      ansible.builtin.pause:
        seconds: 1
