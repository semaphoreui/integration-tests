# Semaphore UI Issue Registry

**Analysis start date:** 2026-08-06
**Repository:** https://github.com/semaphoreui/semaphore
**Current scope:** an initial verified sample — recent bug issues, open tickets labeled `critical`, and several closed/fixed defects used to validate the format.

## How to read the table

* **Date** — issue creation date.
* **What broke** — observed behavior, rather than the assumed root cause.
* **What/how it was fixed** is filled in only when there is a PR, commit, diff, or an unambiguous maintainer comment.
* `Not established` means that a fix may exist, but it cannot be reliably linked to the ticket based on the available data.
* Components correspond to the [component map](./semaphore-testing-component-map.md).

## Initial table

| Date       | Ticket / status                                                                 | User complaint                                                                                                                   | What broke                                                                                                                             | What was fixed                                                                                                                                                                                     | How it was fixed                                                                                                                                                                                   | Component                                                                            |
| ---------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 2026-07-21 | [#4075](https://github.com/semaphoreui/semaphore/issues/4075) · open            | When creating a Cron or Run once schedule, the specified template variables disappear immediately after saving                   | Variables are empty when reopening the form and are not passed to the scheduled job                                                    | Not fixed yet                                                                                                                                                                                      | No fix/PR found                                                                                                                                                                                    | `SCHEDULES`; additionally `TEMPLATES`, `UI`, `Regression`                            |
| 2025-01-08 | [#2682](https://github.com/semaphoreui/semaphore/issues/2682) · open · critical | Ansible 2.18 included in the Docker image is incompatible with Python 3.6 on managed RHEL 8 hosts                                | Ansible modules fail with `SyntaxError: future feature annotations is not defined`; the standard image cannot be used for such targets | No complete solution in the issue; an image with Ansible 2.16 was provided as an option, and users also use custom images                                                                          | The maintainer suggested a custom Dockerfile; several images/Execution Environments are being discussed. The issue remains open                                                                    | `EXECUTORS`; additionally `DEPLOYMENT`, `Compatibility`, `Ansible`                   |
| 2025-01-08 | [#2681](https://github.com/semaphoreui/semaphore/issues/2681) · open · critical | After a successful OIDC login with an empty `web_host`, the user is redirected to a 404 instead of the UI                        | The session is created, but the redirect is built as a relative path, causing the browser to open `/api/auth/oidc/.../project/1`       | A fix was prepared but had not been merged as of the snapshot date                                                                                                                                 | [PR #3831](https://github.com/semaphoreui/semaphore/pull/3831): extracts `buildOidcRedirectURL`, forces a leading `/` when `web_host` is empty, and adds table-driven tests in `api/login_test.go` | `AUTH`; additionally `CONFIG`, `API`, `Regression`                                   |
| 2024-08-19 | [#2294](https://github.com/semaphoreui/semaphore/issues/2294) · open · critical | Semaphore starts tasks on its own and too frequently; some tasks run indefinitely                                                | The scheduled task lifecycle is broken: unexpected executions and stuck runs occur                                                     | Not fixed yet; the cause has not been established                                                                                                                                                  | No maintainer response, PR, or reproducible steps in the issue                                                                                                                                     | `SCHEDULES`; additionally `TASKS`, `Concurrency`, `needs reproduction`               |
| 2024-08-19 | [#2293](https://github.com/semaphoreui/semaphore/issues/2293) · open · critical | A secret cannot be renamed in Environment: the UI reports success, but the old name remains                                      | The secret name change is not persisted; there is no error in the browser console                                                      | Not fixed yet                                                                                                                                                                                      | No fix/PR found; the request payload, API update, and database persistence need to be checked                                                                                                      | `SECRETS`; additionally `UI`, `API`, `Postgres`                                      |
| 2024-07-01 | [#2152](https://github.com/semaphoreui/semaphore/issues/2152) · open · critical | `SEMAPHORE_DB_PORT` is ignored inside the container when Postgres uses a non-standard port                                       | The application continues to use port 5432 regardless of the env var                                                                   | Not fixed yet                                                                                                                                                                                      | No fix/PR found; the likely area to investigate is env/config conversion and the Docker entrypoint, but the cause has not been confirmed                                                           | `CONFIG`; additionally `DEPLOYMENT`, `DB`, `Postgres`                                |
| 2024-06-20 | [#2125](https://github.com/semaphoreui/semaphore/issues/2125) · open · critical | After upgrading to v2.10.7, all schedules stopped running                                                                        | Cron schedules are ignored and tasks are not created                                                                                   | Not fixed yet; insufficient data                                                                                                                                                                   | No logs, maintainer response, or related PR in the issue                                                                                                                                           | `SCHEDULES`; additionally `TASKS`, `Regression`, `needs reproduction`                |
| 2024-06-13 | [#2097](https://github.com/semaphoreui/semaphore/issues/2097) · open · critical | After an upgrade, the Task Log link in email alerts became unclickable and no longer contains the configured `web_host`          | The alert generates an incorrect task URL                                                                                              | Not fixed yet                                                                                                                                                                                      | No fix/PR found; the ticket points to a regression after v2.9.112 and a possible connection with #2084                                                                                             | `OUTPUT`; additionally `CONFIG`, `Alerts`, `Regression`                              |
| 2024-05-07 | [#1999](https://github.com/semaphoreui/semaphore/issues/1999) · open · critical | After upgrading from 2.9.37 to 2.9.75, a template with cron `*/2 * * * *` gets stuck in Waiting every day                        | A subsequent scheduled run does not start; stopping the waiting job and rebooting helps                                                | Not established; the maintainer could not reproduce it                                                                                                                                             | No fix or PR specified; the template configuration, parallel task limits, DB, and scheduler/task pool logs need to be checked                                                                      | `TASKS`; additionally `SCHEDULES`, `Concurrency`, `Regression`, `needs reproduction` |
| 2023-09-05 | [#1459](https://github.com/semaphoreui/semaphore/issues/1459) · closed          | A Project Manager could promote themselves to Owner, demote an Owner, or remove them from the project                            | The server-side project role model and privilege-escalation boundary were broken                                                       | The ticket is closed, but evidence of a specific fix has not yet been found                                                                                                                        | No linked PR/commit; before considering it fixed, the history of `api/projects/users.go` should be checked and direct API tests for the role matrix should be written                              | `PROJECTS`; additionally `RBAC`, `Security`, `Privilege escalation`                  |
| 2023-04-19 | [#1216](https://github.com/semaphoreui/semaphore/issues/1216) · closed          | After a task completed, output lines changed order; `PLAY RECAP` appeared among earlier TASK entries                             | Lines with the same second were sorted ambiguously, causing the persisted history to differ from the live output                       | The maintainer marked the issue as fixed, but the related diff was not specified                                                                                                                   | The approach proposed in the issue was to sort `task__output` by `id` rather than by time. The implementation in the current query path still needs to be confirmed                                | `OUTPUT`; additionally `DB`, `Data integrity`                                        |
| 2023-04-12 | [#1211](https://github.com/semaphoreui/semaphore/issues/1211) · open, fix found | When two cron tasks were started simultaneously, Semaphore using BoltDB crashed with `panic: Connection schedule already exists` | Parallel `ScheduleRunner` instances used the same non-persistent connection name, `schedule`                                           | Fixed in commit [`e2f43bee`](https://github.com/semaphoreui/semaphore/commit/e2f43bee7e4bb13bc0553d371f9ea162e3861c22); the user confirmed that the issue no longer occurred starting with v2.9.75 | The connection name was changed from the shared `schedule` to the unique `schedule <scheduleID>`, preventing parallel schedules from conflicting                                                   | `SCHEDULES`; additionally `DB`, `BoltDB`, `Concurrency`                              |

## Initial conclusions

### 1. Schedules and task lifecycle are the most prominent cluster in the initial sample

The small verified sample already includes:

* variables being lost when saving a schedule (#4075);
* tasks starting too frequently/on their own (#2294);
* completely ignored cron schedules (#2125);
* a task getting stuck in Waiting (#1999);
* a BoltDB crash during concurrent execution (#1211).

This already justifies an early test set covering `create schedule -> persist params -> trigger -> create exactly one task -> complete`, including two schedules at the same time and different DB modes.

### 2. Configuration errors often manifest far away from the configuration layer

An empty `web_host` breaks the OIDC redirect (#2681), the same class of setting is involved in email alert links (#2097), and the DB port provided through the environment does not reach the database connection (#2152). Table-driven configuration tests are needed, along with several integration smoke scenarios.

### 3. GitHub status cannot be used as the sole source of truth

* #1211 is still open even though there is a fix commit and user confirmation.
* #2681 is open and has a ready but unmerged PR.
* #1459 is closed, but no link to a fix is specified.

For further processing, the timeline, related PRs/commits, and current code should be checked for every closed ticket.

### 4. Old `critical` issues require re-triage

Some of them have no logs, reproducible steps, or maintainer response. Their value for the test plan is still high as a risk signal, but they should not automatically be considered confirmed current defects.

## Next pass

1. Analyze the remaining open bug issues by component without performing an in-depth fix analysis.
2. Select 3–5 of the most informative tickets from the `TASKS/SCHEDULES`, `AUTH/RBAC`, `SECRETS`, `REPOSITORIES`, `RUNNERS`, and `MIGRATIONS` clusters.
3. For the selected closed tickets, check the timeline, PR, commit, and current code.
4. Add the fields `close date`, `version`, `DB/install method`, and `root-cause confidence` to the machine-readable CSV once the format is finalized.
